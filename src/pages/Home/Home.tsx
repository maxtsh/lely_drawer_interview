import { Page } from "@/layout";

const Home: React.FC = () => {
  return (
    <Page headerProps={{ heading: "Home" }}>
      <div>
        <h1>Home</h1>
      </div>
    </Page>
  );
};

export default Home;
